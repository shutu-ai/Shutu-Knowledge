# 0.4 Release Gate Tracker

Status: **NOT READY** — implementation and preliminary benchmark progress are
substantial, but final release evidence is incomplete.

Last updated: 2026-09-18.

## Current evidence

| Gate | Status | Evidence / remaining work |
|---|---|---|
| Build | PASS | `go build ./...` passed locally. |
| Go Test | PASS | `go test ./...` passed locally after the committed-delete semantic propagation fix. |
| Race | CONDITIONAL | All non-Knowledge packages passed under race in the full run; `internal/knowledge` hit the default 10-minute package timeout, then passed separately with `go test -race -timeout 20m ./internal/knowledge` (658.472s). A single full `go test -race -timeout 20m ./...` command must still pass. |
| Web | PASS | `npm.cmd run typecheck`, `npm.cmd test`, and `npm.cmd run build` passed; embedded `internal/web/dist` rebuilt. |
| 0.3 KB migration | PRELIMINARY PASS | Migration 0019/0020 additive tests and full storage suite pass; explicit upgraded-0.3-database fixture still needed for final report. |
| 0.3 search regression | PASS (suite) | Full knowledge/search/web suites pass; explicit 0.3-vs-0.4 answer regression report still needed. |
| Knowledge Model | PASS | PH1 model, provenance, relations, versioning, and SQLite storage tests pass. |
| Knowledge Compiler | PASS | PH2 deterministic Fact/Concept/Topic/Summary compiler tests pass. |
| Incremental Compilation | PASS | Dirty queue, source fingerprints, no-op reuse, partial merge, and restart-state tests pass. |
| Delete Propagation | PASS | Shared evidence retention, orphan rejection, active-generation retirement fallback, base cleanup, and committed delete cleanup tests pass. |
| Generation Isolation | PASS | Atomic publication and concurrent active-reader tests pass. |
| Semantic Memory Retrieval | PASS | Concept/Topic/Summary activation, global hierarchy activation, routing diagnostics, and exact evidence tests pass. |
| Context Compiler | PASS | Bounded ContextPackage, section budgets, evidence dedup/drop diagnostics, citations, and token invariant tests pass. |
| Query Routing | PASS | Fact/local/global/cross-document/comparison/multi-hop/temporal deterministic routing tests pass. |
| Fact Queries | PASS (preliminary) | Benchmark family passes without regression. |
| Global Queries | PASS (preliminary) | Quality proxy improves from 1.000 to 2.000. |
| Cross-document Queries | PASS (preliminary) | Quality proxy improves from 0.500 to 1.000. |
| Multi-hop Queries | PASS (preliminary) | Quality proxy improves from 1.000 to 1.500 without relation-table expansion. |
| Temporal Queries | PARTIAL | Conservative versioned supersession and current-query exclusion pass; same-version contradiction and richer historical cases remain. |
| Evidence Citation | PASS | Context and Wiki provenance tests require exact document generation/node/chunk identity. |
| Knowledge Provenance | PASS | Derived-from closure and orphan rejection tests pass. |
| Benchmark vs 0.3 | PRELIMINARY PASS | Aggregate support proxy 4.500 → 6.500 (+44.4%). Final benchmark is not release-grade yet. |
| Context Efficiency | FAIL (current metric) | Preliminary aggregate tokens are 454 → 2,151; semantic context is richer but not more token-efficient. Equal-quality/equal-token benchmark remains required. |
| Restart Recovery | PARTIAL | Durable queue and generation tests pass; explicit kill/restart compilation recovery scenario still needed. |
| Agent Integration | PASS | Existing public extension contract exposes read-only `knowledge_compile_context`; extension tests pass. |
| Windows Package Smoke | NOT RUN | Formal version/package decision pending. |
| CI push/tag | NOT RUN | No 0.4 candidate pushed/tagged. |
| No P0 | UNEVALUATED | Requires final stabilization audit. |

## Blocking work before READY

1. Run one full race command with a 20-minute timeout.
2. Add the required 0.2 comparison to the benchmark.
3. Expand benchmark to the document-intelligence corpus and SmartCare/telecom
   scenario corpus.
4. Add same-version contradiction and richer historical temporal cases.
5. Prove context efficiency under equal answer quality/token budgets, or revise
   only with evidence that another internalization dimension justifies 0.4.
6. Add explicit 0.3 database upgrade fixture and post-migration search evidence.
7. Add an explicit semantic compilation kill/restart recovery test.
8. Run Windows formal package build and package smoke.
9. Push candidate, obtain push/tag CI, and resolve any P0.
10. Produce the final report with Research, Architecture, Benchmark, Migration,
    Regression, CI, Artifact, Known Limitations, Deferred, and READY/NOT READY.
