# 0.4 Release Gate Tracker

Status: **NOT READY** — the `0.4.0-rc.1` candidate and its Windows package are validated locally; push/tag CI and final release closure remain.

Last updated: 2026-09-18.

## Current evidence

| Gate | Status | Evidence / remaining work |
|---|---|---|
| Build | PASS | `go build ./...` passed locally. |
| Go Test | PASS | `go test ./...` passed locally after the committed-delete semantic propagation fix. |
| Race | PASS | One full `go test -race -timeout 20m ./...` command passed (`internal/knowledge`: 666.815s). |
| Web | PASS | `npm.cmd run typecheck`, `npm.cmd test`, and `npm.cmd run build` passed; embedded `internal/web/dist` rebuilt. |
| 0.3 KB migration | PASS | Explicit 0.3 database downgrade/upgrade fixture reaches schema 0020 without rebuilding evidence or changing chunk/generation identity; full storage suite passes. |
| 0.3 search regression | PASS | Full knowledge/search/web suites pass, and the explicit 0.2/0.3/0.4 benchmark harness confirms non-regression. |
| Knowledge Model | PASS | PH1 model, provenance, relations, versioning, and SQLite storage tests pass. |
| Knowledge Compiler | PASS | PH2 deterministic Fact/Concept/Topic/Summary compiler tests pass. |
| Incremental Compilation | PASS | Dirty queue, source fingerprints, no-op reuse, partial merge, and restart-state tests pass. |
| Delete Propagation | PASS | Shared evidence retention, orphan rejection, active-generation retirement fallback, base cleanup, and committed delete cleanup tests pass. |
| Generation Isolation | PASS | Atomic publication and concurrent active-reader tests pass. |
| Semantic Memory Retrieval | PASS | Concept/Topic/Summary activation, global hierarchy activation, routing diagnostics, and exact evidence tests pass. |
| Context Compiler | PASS | Bounded ContextPackage, section budgets, evidence dedup/drop diagnostics, citations, and token invariant tests pass. |
| Query Routing | PASS | Fact/local/global/cross-document/comparison/multi-hop/temporal deterministic routing tests pass. |
| Fact Queries | PASS | Benchmark family passes without regression. |
| Global Queries | PASS | Equal-budget quality proxy improves from 1.000 to 1.667. |
| Cross-document Queries | PASS | Quality improves at the default budget and does not regress under the equal-token stress budget. |
| Multi-hop Queries | PASS | Equal-budget quality proxy improves from 1.000 to 1.500 using bounded sequence relations. |
| Temporal Queries | PASS | Version supersession, current-query exclusion, same-version conflict handling, and explicit historical recall tests pass. |
| Evidence Citation | PASS | Context and Wiki provenance tests require exact document generation/node/chunk identity. |
| Knowledge Provenance | PASS | Derived-from closure and orphan rejection tests pass. |
| Benchmark vs 0.2/0.3 | PASS | Immutable `v0.2.1`/0.3 proxy is 4.500 at 454 tokens; 0.4 is 5.667 at 436 tokens (+25.9% quality, −18 tokens). |
| Context Efficiency | PASS | Under the per-query equal-token cap, aggregate quality improves while aggregate tokens fall; evidence remains reserved at minimum budget 64. |
| Restart Recovery | PASS | Explicit close/reopen pending-change recovery and offline packaged restart retrieval tests pass. |
| Agent Integration | PASS | Existing public extension contract exposes read-only `knowledge_compile_context`; extension tests pass. |
| Windows Package Smoke | PASS | Formal `0.4.0-rc.1` ZIP passed startup, Markdown/OCR/DOCX/PPTX/XLSX import, embedding, online vector retrieval, runtime status, and offline restart retrieval using a prevalidated local model cache. |
| CI push/tag | NOT RUN | No 0.4 candidate pushed/tagged. |
| No P0 | UNEVALUATED | Requires final stabilization audit. |

## Blocking work before READY

1. Push the validated candidate and obtain push CI.
2. Obtain runtime/release-host/tag CI for the selected release tag.
3. Complete the final-version/package decision and, if promoting beyond RC,
   rebuild and revalidate the formal package from that exact source.
4. Produce the final report and complete the requirement-by-requirement no-P0
   audit.
