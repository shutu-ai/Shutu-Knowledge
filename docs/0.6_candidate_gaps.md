# 0.6 Candidate Gaps

This list is derived only from the 2026-09-19 248-query real-world temporal run.
The overall classification is **ACCEPTABLE**: 0.5 is materially better, but one
high-value gap remains.

**Recommendation: START 0.6 ONLY FOR TOP GAP.** Do not start a broad 0.6
architecture change.

| Priority | Candidate | Observed evidence | Frequency | Impact | Root cause | Possible direction | Expected gain | Complexity | Risk |
|---|---|---|---:|---|---|---|---|---|---|
| PRIMARY | Ambiguous temporal range/history planning | The repeated “earlier Open5GS release stages” query omitted the expected release evidence in every repetition. | 8/8 cases; 5.4 pp of the total temporal gap | Ambiguous historical questions can miss the exact versioned evidence even when provenance is valid. | The router resolves neither an explicit identity nor a bounded multi-version range, so scope selection has no historical target. | A narrow planner for bounded, ambiguous temporal ranges that expands to provenanced version identities and preserves fail-closed behavior. | Up to +5.4 pp strict temporal accuracy and 8/8 on the controlled gap. | Medium | Must not broaden ambiguous queries into unbounded history or invent versions. |
| SECONDARY | Current-version anchor selection | Four failures select generic pages, draft-family files, or an older report instead of the current anchor. | 4/12 cases; 2.7 pp of the total temporal gap | “Current” can return plausible but non-current evidence. | Current identity exists, but the ranking lacks a stable document/title/version anchor for vague subjects and draft families. | Current-family grouping with provenance-backed title/version anchors and conservative draft promotion. | Could recover most or all of the 4 misses after the primary gap. | Medium | Wrong promotion is riskier than refusing to resolve. |
| DEFERRED | Parser-native effective/revision dates | Only one Introduced-In and one Explicit Version miss remain after targeted fixes. | 2/148 cases | Modest accuracy loss and weaker CJK/PDF generality. | Version evidence can depend on filename/title conventions rather than parsed dates. | Add dates only when parser extraction has auditable provenance and a measured false-metadata gate. | Incremental robustness; not the immediate blocker. | High | Fabricated or conflicting dates could harm supersession. |

## Entry condition for 0.6

Start only the primary candidate when a bounded implementation can show:

1. A regression suite covering ambiguous multi-version ranges.
2. No false supersession or fabricated metadata.
3. Strict temporal accuracy at least 95.3% (141/148) on this suite.
4. No regression below 100% support on the five non-temporal categories.
5. No material increase in context tokens.

All other observed items remain candidates for later measurement, not immediate
development.
